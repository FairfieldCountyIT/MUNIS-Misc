# TimeClock+ Export Files
These files, when executed, create export files per file layouts required by DMI for import into TimeClock+.

Required TC+ modules are AutoImport Utility, LDAP, WebClock, and MUNIS.
